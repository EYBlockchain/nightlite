{
  "name": "nightfall-sdk-api",
  "description": "SDK for Nightfall",
  "license": "MIT",
  "version": "0.0.0",
  "scripts": {
    "start": "node build/index.js",
    "dev": "nodemon build/index.js",
    "fix": "npm run format && npm run lint -- --fix && solium -d contracts",
    "format": "cod-scripts format",
    "lint": "cod-scripts lint",
    "lint:md": "markdownlint -i node_modules -i build .",
    "validate": "cod-scripts test",
    "lint:sol": "solium -d contracts"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.6.1",
    "@commitlint/config-conventional": "^7.6.0",
    "cod-scripts": "^2.0.0",
    "eslint": "^5.15.2",
    "eslint-config-codfish": "^2.5.0",
    "ethlint": "^1.2.4",
    "husky": "^1.3.1",
    "lint-staged": "^8.2.1",
    "markdownlint-cli": "^0.18.0",
    "nodemon": "^1.19.2",
    "prettier": "^1.16.4",
    "prettier-plugin-solidity": "^1.0.0-alpha.31"
  },
  "dependencies": {
    "@eyblockchain/dapp-utils": "^1.5.0",
    "config": "^3.2.2"
  },
  "publishConfig": {
    "registry": "https://registry.ey.npme.io/",
    "access": "public"
  },
  "eslintConfig": {
    "extends": [
      "./node_modules/cod-scripts/eslint.js"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "cod-scripts pre-commit",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
